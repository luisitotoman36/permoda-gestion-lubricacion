Frontend React + TypeScript + Tailwind para PERMODA.

Instalación y desarrollo:

```bash
cd frontend
npm install
cp .env.example .env # opcional, para VITE_API_BASE
npm run dev
```

Variables de entorno:
- `VITE_API_BASE` - URL base del backend (por defecto http://localhost:4000/api)
